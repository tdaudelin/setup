# Global tools configuration
PATH="$HOME/.local/bin:$PATH"

# Set PATH, MANPATH, etc., for Homebrew.
# eval "$(/opt/homebrew/bin/brew shellenv)"

# Activate mise (polyglot version manager)
# eval "$(mise activate zsh)"
